apiVersion: v1
kind: Service
metadata:
  name: { include "argocd.fullname" . }
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    {- include "argocd.selectorLabels" . | nindent 4 }
