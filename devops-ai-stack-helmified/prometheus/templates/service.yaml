apiVersion: v1
kind: Service
metadata:
  name: { include "prometheus.fullname" . }
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    {- include "prometheus.selectorLabels" . | nindent 4 }
