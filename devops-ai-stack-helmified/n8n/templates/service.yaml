apiVersion: v1
kind: Service
metadata:
  name: { include "n8n.fullname" . }
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    {- include "n8n.selectorLabels" . | nindent 4 }
