apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: { include "n8n.fullname" . }
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: n8n.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: { include "n8n.fullname" . }
                port:
                  number: 80
