apiVersion: v1
kind: Service
metadata:
  name: { include "cert-manager.fullname" . }
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    {- include "cert-manager.selectorLabels" . | nindent 4 }
