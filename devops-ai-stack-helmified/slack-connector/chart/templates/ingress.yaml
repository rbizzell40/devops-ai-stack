apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: { include "slack-connector.fullname" . }
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: slack-connector.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: { include "slack-connector.fullname" . }
                port:
                  number: 80
