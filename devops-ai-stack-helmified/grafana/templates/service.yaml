apiVersion: v1
kind: Service
metadata:
  name: { include "grafana.fullname" . }
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    {- include "grafana.selectorLabels" . | nindent 4 }
