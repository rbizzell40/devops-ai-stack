apiVersion: v1
kind: Service
metadata:
  name: { include "telegram-bot.fullname" . }
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    {- include "telegram-bot.selectorLabels" . | nindent 4 }
